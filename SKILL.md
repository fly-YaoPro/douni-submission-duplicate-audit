---
name: douni-submission-duplicate-audit
description: 豆尼·银手的每日提报查重模块。用于提报查重、达人查重、检查当日重复提报、执行中、黑名单、提报中和客户否过；按已配置调度读取当天独立提报表并只把结果写回该表。
---

# 提报查重

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill douni-submission-duplicate-audit
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 数据边界

- 候选池：每日提报表/<M月D日提报>，该表必须由 douni-full-base-sync 按 `sources.submissions.viewTemplate` 指定模板创建；查重只处理该表中业务日期对应且达人名称非空的记录。
- 历史执行证据：13 张机构表同步形成的全量表历史记录。
- 写回目标：仅当天独立提报表的查重结果、提报客户反馈，必要时写提报审核=内审不通过。
- 本 Skill 禁止直接写机构表或全量表。未提报日表负责内审和选题；向客户提报入口只把内审不通过及选题/备注回机构。客户终审由客户结果回填入口先写机构再写原日表；半小时镜像仅处理已提交且精确匹配的合作，不覆盖未提报内审内容。

具体 Base、全量表、黑名单与客户提报 Sheet 由公共上下文的 `sources.department`、`sources.submissions` 提供；不从文档中的历史 ID 恢复来源。

## 判断规则

1. 使用仓库 `src/services/submission-creator-identity.ts` 分级判定：同平台主页账号 ID 一致可确认改名；双方账号 ID 不同或平台不同不合并，短链/作品链接不当账号 ID。昵称先统一 NFKC、大小写、空白/不可见字符，再建立去标点/emoji 的文字键（保留汉字、字母、数字、括号内文字）。完整文字键一致且至少 4 字符、无多账号冲突时按同一人；原有完整同名匹配保留，但已知账号冲突优先。过短的装饰昵称、截断/缺字/错字、同名多账号且缺少双方主页证据时仅列身份待确认，不凭相似度自动否决，不自动保存别名。完全改名且无账号证据不能保证识别。该规则统一用于当日重复、历史执行、黑名单、四路客户提报历史。
2. 当日日提报表内同名超过一条：查重结果=重复。
3. 历史执行中：业务日以前存在客户通过记录，且进度不是已发布、暂停、取消；或客户表存在尚未结束的历史通过批次。历史日期优先使用「提报时间」；该字段为空但「提报客户时间」有值时，必须以后者作为历史日期，不得丢弃该执行证据。写执行中与提报审核=内审不通过。
4. 黑名单同名：写已拉黑与提报审核=内审不通过。
5. 客户表截至业务日最新一批同名记录：
   - 审核全部为空：写提报中与提报审核=内审不通过；
   - 明确否/不通过/拒绝：写客户否过，并带回客户反馈原文；
   - 明确是/通过时，不被同批空白行覆盖。
6. 最终优先级：已拉黑 > 执行中 > 提报中 > 客户否过 > 重复。
7. 客户反馈独立保留；即使最终结论优先显示为已拉黑或执行中，只要客户侧同时否过，仍写反馈。
8. 无异常时清空日提报表中旧的查重结果和客户反馈；不自动把提报审核恢复为通过或空白。
9. 已提交/客户终态/提交台账待确认的记录保留现值，不降级或清空。只有身份疑点而无确定命中时整条不写，单列身份待确认；有确定命中仍照常处理并另列疑点，不把不确定候选的审核或反馈带入。

## 执行门禁

- 按 Daemon 当前查重计划运行。执行器统一读取 `data/submission-audit-inputs/<业务日期>/ready.json` 指向的已核验输入：当日14:00候选收集回执，以及当日14:00后结构化全量字段核验通过的不可变历史快照。附件、表名或工作台导出失败不阻断这份已核验输入；来源、日期、字段、数量或哈希缺失/不一致仍暂停。新凭据尚未生成时不回退旧数据、不伪造成功。
- 固定消费 14:00 已落表的候选集合；14:00 后机构新增行不进入当天查重。
- 写入后逐条回读；失败不发送成功卡片。
- 客户历史读取使用查重专用字段校验：身份、平台、提报日期、审核、反馈、选题及小红书换选题/方向字段必须唯一且完整；不因未用于查重的备注、报价等列缺失而停止。客户提报写入继续使用完整字段校验，不能借此放宽写入门禁。错误须标明具体来源。
- 按 Daemon 当前查重计划正式卡片发原内部群；仅在遥师明确要求当天测试或预览时改发遥师私聊。确定命中保留顶部数据和总计；“达人身份待确认”区单列当前机构、唯一 ID、原昵称、平台，以及疑似原昵称、来源表/行或历史唯一 ID、原因、来源链接。待确认数不算确定命中；请同事核对后补全主页/昵称再重跑，不虚构确认。按钮打开当天日表，历史日表提醒保持不变。
- 卡片底部同时扫描“每日提报表”文件夹内早于当天的全部日提报表，并排除当天表：
  - `提报审核` 为“客户通过”或“客户不通过”时算客户二审完成；“内审不通过”表示无需进入客户二审，也从待客户二审数中排除；其他值或空白才算未完成；
  - 未全部处理完的表显示待客户二审剩余条数、客户二审完成数、内审不通过数、总数和表格直达链接；
  - 全部记录均已得到客户二审结果或明确内审不通过的表显示“可以删除”和表格直达链接；
  - 这里只提醒，不自动删除历史表。

## 运行入口

```powershell
# 只读预览
node node_modules/tsx/dist/cli.mjs scripts/run-submission-duplicate-audit.ts --date YYYY-MM-DD

# 正式写入当天独立提报表
node node_modules/tsx/dist/cli.mjs scripts/run-submission-duplicate-audit.ts --date YYYY-MM-DD --apply
```
